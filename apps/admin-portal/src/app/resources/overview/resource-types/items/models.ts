import { Item } from '@craftsmans-ledger/shared-ui';
import { TEMP_RESOURCE_ID } from '../../../models';

export const TEMP_ITEM: Item = {
    id: TEMP_RESOURCE_ID,
    name: null,
    weight: null,
    baseValue: null,
};
